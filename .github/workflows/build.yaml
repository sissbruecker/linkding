name: build

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version from file
        id: get_version
        run: echo "VERSION=$(cat version.txt)" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build latest
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/default.Dockerfile
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          tags: |
            b13rg/linkding:latest
            b13rg/linkding:${{ env.VERSION }}
            ghcr.io/b13rg/linkding:latest
            ghcr.io/b13rg/linkding:${{ env.VERSION }}
          target: linkding
          push: true
          cache-from: type=registry,ref=ghcr.io/b13rg/linkding:buildcache
          cache-to: type=registry,ref=ghcr.io/b13rg/linkding:buildcache,mode=max

      - name: Build latest-alpine
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/alpine.Dockerfile
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          tags: |
            b13rg/linkding:latest-alpine
            b13rg/linkding:${{ env.VERSION }}-alpine
            ghcr.io/b13rg/linkding:latest-alpine
            ghcr.io/b13rg/linkding:${{ env.VERSION }}-alpine
          target: linkding
          push: true
          cache-from: type=registry,ref=ghcr.io/b13rg/linkding:buildcache-alpine
          cache-to: type=registry,ref=ghcr.io/b13rg/linkding:buildcache-alpine,mode=max

      - name: Build latest-plus
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/default.Dockerfile
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          tags: |
            b13rg/linkding:latest-plus
            b13rg/linkding:${{ env.VERSION }}-plus
            ghcr.io/b13rg/linkding:latest-plus
            ghcr.io/b13rg/linkding:${{ env.VERSION }}-plus
          target: linkding-plus
          push: true
          cache-from: type=registry,ref=ghcr.io/b13rg/linkding:buildcache
          cache-to: type=registry,ref=ghcr.io/b13rg/linkding:buildcache,mode=max

      - name: Build latest-plus-alpine
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/alpine.Dockerfile
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          tags: |
            b13rg/linkding:latest-plus-alpine
            b13rg/linkding:${{ env.VERSION }}-plus-alpine
            ghcr.io/b13rg/linkding:latest-plus-alpine
            ghcr.io/b13rg/linkding:${{ env.VERSION }}-plus-alpine
          target: linkding-plus
          push: true
          cache-from: type=registry,ref=ghcr.io/b13rg/linkding:buildcache-alpine
          cache-to: type=registry,ref=ghcr.io/b13rg/linkding:buildcache-alpine,mode=max